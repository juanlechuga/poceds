// blocks/telcel-card-pack/telcel-card-pack.js
// Bloque vacío: sólo registra la clase para confirmar carga.
export default function decorate(block) {
  block.classList.add('telcel-card-pack');
  // no renderizamos nada todavía
}
